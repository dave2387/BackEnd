// Create a program that joins "users", "arjun", "documents", "project" into a single path using path.join().

const path = require('path');

console.log(path.join("users", "arjun", "documents", "project"));
